{
  "name": "@echon-dev/root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.0.2"
  },
  "scripts": {
    "build": "lerna run build",
    "prepublish": "npm run build",
    "publish": "lerna publish -- --access public"
  }
}
